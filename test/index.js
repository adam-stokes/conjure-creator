import assert from 'assert';
// import conjureCreator from '../lib';

describe('conjure-creator', function () {
  it('should have unit test!', function () {
    assert(true, 'we expected this package author to add actual unit tests.');
  });
});
